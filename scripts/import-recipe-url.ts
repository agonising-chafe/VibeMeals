/**
 * Disabled recipe import script.
 * The automated scraper pipeline was removed. Use manual, spec-driven entry only.
 */

console.error('Recipe import is disabled. Add recipes manually per docs/recipe-spec.md and docs/data-model.md.');
process.exit(1);
